export default function RunPage(_props: { params: Promise<{ id: string }> }) {
  return (
    <div>
      <h1 className="font-display text-2xl text-text-primary">Run Detail</h1>
      <p className="text-text-muted">Route view placeholder</p>
    </div>
  );
}
